<h2>My communities</h2>

<table class="subscribed">
<% @memberships.each do |membership| %>
  <tr>
    <td><%= membership.community.name %></td>
    <td><%= link_to 'unsubscribe', membership_path(membership), method: :delete, remote: true%></td>
  </tr>
  <% end %>
</table>