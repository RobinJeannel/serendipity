<%= form_for([@community, @article]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title,  :placeholder => @article.title %>
  <%= f.label :content %>
  <%= f.text_area :content,:placeholder => @article.content, class: 'editable' %>
  <%= f.submit %>
<% end %>

<%= link_to 'Post my artcile'%>

f.sir_trevor_text_area :content
<%= render_sir_trevor(post.content) %>
